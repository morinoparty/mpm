---
title: Plugin Management
description: プラグインのインストール、削除、一覧表示などを行うコマンドです
---

プラグインのインストール、削除、一覧表示などを行うコマンドです。

## コマンド一覧

インストール、アンインストール、一覧表示、更新管理に関するコマンドです。

<CommandList category="manage" />

## ワークフロー

プラグイン管理の基本的なワークフローです。

### 1. リポジトリファイルを作成

手動で `repository/QuickShop.json` を作成します。

### 2. プラグインを管理対象に追加

```bash
/mpm add QuickShop
```

`mpm.json` にプラグイン情報が追加されます。

### 3. インストール

```bash
/mpm install
```

プラグインがダウンロードされ、`plugins/` ディレクトリに配置されます。

### 4. サーバーを再起動

```bash
/restart
```

プラグインが有効化されます。

## トラブルシューティング

### プラグインがインストールされない

1. **リポジトリファイルを確認**
   ```bash
   # ファイルが存在するか確認
   ls plugins/MinecraftPluginManager/repository/
   ```

2. **mpm.json を確認**
   ```bash
   # プラグインが追加されているか確認
   cat plugins/MinecraftPluginManager/mpm.json
   ```

3. **ログを確認**
   - サーバーログでエラーメッセージを確認してください

### 管理外プラグインが削除される

mpm管理下にないプラグインを保持するには：

```bash
# local/ ディレクトリに移動
mv plugins/YourPlugin.jar plugins/MinecraftPluginManager/local/
```

`local/` ディレクトリ内のプラグインは `mpm removeUnmanaged` コマンドでも削除されません。

### インストール済みプラグインの確認

```bash
# すべてのプラグインを表示（デフォルト）
/mpm list

# すべてのプラグインを表示（明示的）
/mpm list --all

# 管理中のプラグインのみ表示
/mpm list --managed

# 管理外のプラグインのみ表示
/mpm list --unmanaged

# ロック中のプラグインのみ表示
/mpm list --locked
```

## 関連ドキュメント

- [プラグイン操作コマンド](/docs/commands/plugin) - プラグインの情報表示・ローディング
- [リポジトリ管理コマンド](/docs/commands/repo) - リポジトリファイルの作成・管理
- [バックアップコマンド](/docs/commands/backup) - プラグインのバックアップ・リストア
- [依存関係コマンド](/docs/commands/dependency) - プラグインの依存関係解析
- [はじめに](/docs/usage/getting-started) - 基本的な使い方
- [プラグインを管理する](/docs/usage/install) - 詳細なワークフロー
