---
title: Repository Management
description: プラグインのダウンロード元を管理するリポジトリファイルを作成・管理するコマンドです
---

import { Callout } from "fumadocs-ui/components/callout";

プラグインのダウンロード元を管理するリポジトリファイルを作成・管理するコマンドです。

## コマンド一覧

<CommandList category="repo" />

## ワークフロー

リポジトリファイルを使った基本的なワークフロー：

### 1. リポジトリファイルを作成

<Callout type="warn">
⚠️ `mpm create-repo`コマンドは未実装です。手動でJSONファイルを作成するか、config.jsonにリポジトリURLを追加してください。
</Callout>

手動でJSONファイルを作成：

```json
{
    "id": "QuickShop",
    "website": "https://modrinth.com/plugin/quickshop-hikari",
    "repositories": [
        {
            "type": "modrinth",
            "id": "quickshop-hikari"
        }
    ]
}
```

### 2. プラグインを追加

```bash
/mpm add QuickShop
```

### 3. インストール

```bash
/mpm install
```

## トラブルシューティング

### ファイル選択パターン（fileNameRegex）が機能しない

1. **正規表現の構文を確認**
   ```json
   // 正しい例
   "fileNameRegex": "EssentialsX-.*\\.jar"
   "fileNameRegex": "QuickShop-Hikari-[0-9\\.]+\\.jar"

   // エスケープを忘れないように（ドットは \\.）
   ```

2. **パターンをシンプルにしてテスト**
   ```json
   // まずシンプルなパターンから
   "fileNameRegex": ".*\\.jar"
   ```

### リポジトリファイルが見つからない

```bash
# ディレクトリ構造を確認
ls plugins/mpm/repository/

# ファイル名がプラグインIDと一致しているか確認
cat plugins/mpm/repository/QuickShop.json
```

## 関連ドキュメント

- [プラグイン管理コマンド](/docs/commands/manage) - プラグインのインストール・削除
- [リポジトリファイル形式](/docs/format/repository) - ファイル形式の詳細
- [はじめに](/docs/usage/getting-started) - 基本的な使い方
